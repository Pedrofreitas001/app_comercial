import { Package } from "lucide-react";
import { PagePlaceholder } from "@/components/page-placeholder";

export default function ProdutosPage() {
  return (
    <PagePlaceholder
      icon={Package}
      title="Produtos"
      description="Cadastro e importação do mapa de SKUs (DIM_V1) chegam na próxima etapa."
    />
  );
}
