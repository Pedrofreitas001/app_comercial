import { Users } from "lucide-react";
import { PagePlaceholder } from "@/components/page-placeholder";

export default function ClientesPage() {
  return (
    <PagePlaceholder
      icon={Users}
      title="Clientes"
      description="Cadastro e importação da base de clientes chegam na próxima etapa."
    />
  );
}
